<!DOCTYPE html>
<html>
<head>
  <title><%= pritem._id ? 'Edit PR Item' : 'Add PR Item' %></title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/stylesheets/Report.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <header class="no-print">
    <% include index.ejs %>
  </header>  
  <h1><%= pritem._id ? 'Edit PR Item for PR #' + (pr.PRno || pr._id) : 'Add PR Item to PR #' + (pr.PRno || pr._id) %></h1>
  <form action="/pr/<%= pr._id %>/pritem/<%= pritem._id %>/edit" method="POST">
    <div class="mb-3">
      <label for="quantity" class="form-label">Quantity</label>
      <input class="form-control" type="text" id="quantity" name="quantity" value="<%= pritem.quantity %>" />
    </div>
    <div class="mb-3">
      <label for="unit" class="form-label">Unit</label>
      <input class="form-control" type="text" id="unit" name="unit" value="<%= pritem.unit %>" />
    </div>
    <div class="mb-3">
      <label for="sn" class="form-label">SN</label>
      <input class="form-control" type="text" id="sn" name="sn" value="<%= pritem.sn %>" />
    </div>
    <div class="mb-3">
      <label for="description" class="form-label">Description</label>
      <textarea class="form-control" id="description" name="description"><%= pritem.description %></textarea>
    </div>
    <div class="mb-3">
      <label class="form-label">Stock Location</label><br/>
      <div class="form-check form-check-inline">
        <input class="form-check-input pritem" type="radio" name="stockLocation" id="instockRadio" value="instock"
          <%= pritem.instock === '/' ? 'checked' : '' %>>
        <label class="form-check-label" for="instockRadio">In Stock</label>
      </div>
      <div class="form-check form-check-inline">
        <input class="form-check-input pritem" type="radio" name="stockLocation" id="outstockRadio" value="outstock"
          <%= pritem.outstock === '/' ? 'checked' : '' %>>
        <label class="form-check-label" for="outstockRadio">Out Stock</label>
      </div>
    </div>
    <div class="mb-3">
      <label for="price" class="form-label">Price</label>
      <input class="form-control" type="text" id="price" name="price" value="<%= pritem.price %>" />
    </div>
    <div class="mb-3">
      <label for="ppu" class="form-label">PPU</label>
      <input class="form-control" type="text" id="ppu" name="ppu" value="<%= pritem.ppu %>" />
    </div>
    <div class="mb-3">
      <label for="remark" class="form-label">Remark</label>
      <textarea class="form-control" id="remark" name="remark"><%= pritem.remark %></textarea>
    </div>
    <div class="mb-3">
      <label for="total" class="form-label">Total</label>
      <input class="form-control" type="text" id="total" name="total" value="<%= pritem.total %>" />
    </div>
    <button type="submit" class="btn btn-primary"><%= pritem._id ? 'Update Item' : 'Add Item' %></button>
  </form>

  <% if (errors) { %>
    <div class="alert alert-danger mt-3">
      <ul>
        <% for (let field in errors) { %>
          <li><%= field %>: <%= errors[field].message || errors[field] %></li>
        <% } %>
      </ul>
    </div>
  <% } %>
  <script src="/javascripts/pritem.js"></script>
</body>
</html>
