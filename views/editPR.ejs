<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit PR</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
  <h2>Edit Purchase Request</h2>
  <form action="/pr/edit/<%= pr._id %>" method="POST" class="mb-3">
    <div class="mb-3">
      <label class="form-label">Name</label>
      <input name="name" class="form-control" value="<%= pr.name %>" required />
    </div>
    <div class="mb-3">
      <label class="form-label">Date</label>
      <input name="date" type="date" class="form-control" value="<%= pr.date?.toISOString().split('T')[0] %>" />
    </div>
    <div class="mb-3">
      <label class="form-label">Note</label>
      <input name="note" class="form-control" value="<%= pr.note %>" />
    </div>
    <button type="submit" class="btn btn-primary">Update</button>
    <a href="/pr/all" class="btn btn-secondary">Cancel</a>
  </form>
</body>
</html>
